# Copy this file to .env and fill in your values.
# Never commit .env to the repo.

# LLM provider (use one or both)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional: model overrides (defaults will be set in pipeline config)
# ANTHROPIC_MODEL=claude-sonnet-4-6
# OPENAI_MODEL=gpt-4o
